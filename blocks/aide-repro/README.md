# Aide-repro Block

## Overview

Test component to verify the "Move to top" behavior described in support ticket E-002404083 / CSC-571. The block renders its own `title` field before the `aide-repro-item` children in the DOM, matching the customer's "aide" block structure that triggers the Universal Editor "Move to top" bug.
